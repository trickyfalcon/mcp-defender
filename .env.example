# Azure AD App Registration credentials
# See: https://learn.microsoft.com/en-us/azure/active-directory/develop/quickstart-register-app

AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id

# Authentication: Use EITHER certificate OR client secret (certificate preferred)

# Option 1: Certificate authentication (recommended)
AZURE_CLIENT_CERTIFICATE_PATH=/path/to/certificate.pem
# AZURE_CLIENT_CERTIFICATE_PASSWORD=optional-password-if-encrypted

# Option 2: Client secret authentication
# AZURE_CLIENT_SECRET=your-client-secret

# Optional: Specify the Defender API endpoint (defaults to commercial)
# DEFENDER_API_ENDPOINT=https://api.security.microsoft.com
